<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notely</title>
    <link
      rel="stylesheet"
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=arrow_back_ios"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="bg-zinc-900 text-white">
    <main class="w-full min-h-screen p-6">
      
      <!-- Cards Section -->
      <section class="max-w-6xl mx-auto">
        <article class="rounded-md bg-zinc-800 p-4 flex flex-col gap-3">

          <a href="/" class="text-blue-400 mb-3 inline-block">
            <span class="material-symbols-outlined"> arrow_back_ios </span>
          </a>

          <h2 class="text-4xl font-semibold mb-5">
            <%= 
            (filename.replace('.txt','').replace(/-/g, ' ')).charAt(0).toUpperCase() + (filename.replace('.txt','').replace(/-/g,' ').slice(1).toLowerCase())%>
          </h2>

          <p class="tracking-tight font-[300] text-xl mt-3"><%= file %></p>

        </article>
      </section>
    </main>

    <script src="/scripts/script.js"></script>
  </body>
</html>
